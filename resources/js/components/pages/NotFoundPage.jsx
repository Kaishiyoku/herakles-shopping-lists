import React from 'react';
import DefaultGrid from '../core/DefaultGrid';

const NotFoundPage = () => {
    return (
        <DefaultGrid>
            Page not found.
        </DefaultGrid>
    );
}

export default NotFoundPage;
